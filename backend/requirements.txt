# ArkManiaGest - Backend Dependencies
# =====================================
# Versioni flessibili per compatibilita con Python 3.12-3.14

# --- Framework ---
# 0.121 is the first release with Depends(..., scope="function"), which
# app/db/session.py needs to commit before the response is sent.  Lowering
# this floor below 0.121 silently restores the 0.118-0.120 ordering (commit
# after the response) -- see _before_response() there.
fastapi>=0.121
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7

# --- Database (MariaDB) ---
sqlalchemy>=2.0
aiomysql>=0.2.0          # Driver async MySQL/MariaDB (pure Python, no Rust)
pymysql>=1.1             # Driver sync (per aiomysql)

# --- SSH ---
paramiko>=3.5

# --- Crittografia vault locale ---
cryptography>=44

# --- Auth ---
PyJWT>=2.9
bcrypt>=4.2

# --- Utility ---
python-dotenv>=1.0
python-multipart>=0.0.12
httpx>=0.28
